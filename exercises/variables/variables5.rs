// variables5.rs
//
// Execute `rustlings hint variables5` or use the `hint` watch subcommand for a
// hint.



fn main() {
    let number = "T-H-R-E-E"; // 保持这行不变
    println!("Spell a Number : {}", number);
    let number_plus_two = 3; // 创建一个新的变量来存储整数
    println!("Number plus two is : {}", number_plus_two + 2);
}
